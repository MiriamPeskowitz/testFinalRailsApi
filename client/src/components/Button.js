//this is a child of navbar
import React from 'react';

const buttons = ({ actions that it needs }) => 

  <div>
      <button 
        type="button"
        onClick={() => pull in the default? }
        className={for styling}
        />
        Home
      </button>

      <button 
        type="button"
        onClick={() => pull data from api index and go to Content }
        className={for styling}
        />
        All
      </button>

      <button 
        type="button"
        onClick={() => load form }
        className={for styling}
        />
        Register
      </button>
  </div>


export default Buttons