// Will import the components needed for content -- the entries 

//put content needs here, then transfer to components 

import React, { Component } from 'react';
import  { fetchEntries } from '../actions/entriesActions'
import Home from './components/Home'


class {this} extends React.Component {
  render() {
    return (
      <div>
        <h1>Navbar render</h1>
      </div>
    )
  }
}


<Home />

//component: has a form to list the entries, in batch of ten EntriesList 
//component: has a form to enter data Name, Email, Entry.title, Entry.content, 

export default Content; 